# SmartProject
demo data store 
Go to Attendance tab and start a session (if not already).
In “Capture face for verification”, click “Take Photo” and confirm the snapshot.
Only then click “Mark Attendance.”
If the camera box is blank or “Take Photo” never appears:

Allow camera access in the browser (lock icon → Allow).
Use Chrome or Edge.
Refresh the page and try again.
If you want, I can add a file upload fallback (so you can upload a face image instead of using the camera). Just say the word and I’ll patch
 
 https://www.youtube.com/watch?v=KBOCCssL5B4&t=567s 